# A script to setup profile
# Goal is to run this script on another machine and you can immediately access all functions from this 
# repo with a config file
# Only concern is that you need a database to access the SQL functions
# but you should be able to turn that off via config